version: '3.8'
services:
  api:
    container_name: youapp-backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '9000:9000'
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run start:dev
